# Start2Rent Frontend

React (Create React App) UI for Start2Rent.

## Run locally

```bash
npm install
npm start
```

## Environment

Set `REACT_APP_API_BASE_URL` in `.env` (defaults to `http://localhost:5000`).
